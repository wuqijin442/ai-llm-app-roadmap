# 2026-09-12 工作日志

## 第 15 期每日自动化（AI 应用安全与对齐三件套）

- 3 岗位：LLM Red Teamer / RAG 投毒防御工程师 / AI 对齐评估工程师（累计 73 岗位 / 15 期）
- 3 仓库 clone+test：promptfoo（25,035★，102/102 .py ✅）/ NVIDIA-NeMo/Curator（1,761★，971/971 ✅ + 20 ipynb）/ openai/evals（19,437★，353/353 ✅ + 5 ipynb）→ 累计 58 仓库
- docs/20 v2 回写 58 仓库（47 全过 / 10 部分通过）；docs/30 累计更新；README 一致性校验通过
- git：commit 14157df，push 33d2bbc..14157df（RC=0）
- ima：文件 1/3、2/3 推送成功；文件 3/3（docs/20）因 ima-mcp token 过期（unauthorized）待重推

## 关键事故与沉淀

1. **宿主运行时迁移破坏托管 Python**：3.13.12 的 Lib/ 标准库整个缺失（`python -E` 也起不来），v1 全量测试 1h34m 数据作废（py_compile 子进程全 0 + API 全 403）。改系统 Python 3.11.1（G:/codeConfig/python/python3.11.1/python.exe）。
2. **测试脚本三补丁**（scripts/test_cloned_projects.py）：启动清 PYTHONHOME/PYTHONPATH、py_compile 进程内化（1h34m→14m51s）、stars 缓存 `.tmp_automation/stars_cache.json`（API 限流回读 + shields.io JSON 兜底）。
3. **会话 shell 不稳定**：shim dirname not found、coreutils 随机缺失；git push 需 `export PATH="/c/Program Files/Git/mingw64/bin:$PATH"` 修 libcurl-4.dll；PowerShell 5.1 对 UTF-8 无 BOM 中文 pattern 匹配失效，改用 Python。
