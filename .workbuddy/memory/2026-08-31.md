# 2026-08-31 工作区日志

## 11:00 ~ 11:20 · 补推 ima 知识库过期累积文档（老大指令「推」）

### 背景
新会话中 `ima-mcp` 生效（上一轮已把 ima-mcp 条目合并写入 `~/.workbuddy/mcp.json`，重启会话后连接器可用）。
执行推送前先查 `get_knowledge_list` 核对现状，避免重复推或漏推。

### 发现：两份累积型文档落后 3 期
| 知识库现有 | 大小 | 本地最新 | 大小 | 内容差距 |
|---|---|---|---|---|
| W35-岗位全景与学习路径索引.md | 13758 B | docs/30 | 28322 B | 20 岗/2 期 → **34 岗/4 期** |
| W35-克隆企业级项目测试报告.md | 11000 B | docs/20 | 17406 B | 11 仓 → **27 仓** |

已推且最新的 5 份（无需重推）：W35 三期 daily-digest、W35-2026-W35-周报.md、W36-daily-digest-2026-08-31.md。

### 根因
自动化任务 prompt 只规定了「daily-digest 按 ISO 周推」，**没有规定累积型文档（docs/30 索引、docs/20 测试报告）何时重推**。
这两份文档每期原地追加，08-29 / 08-30 / 08-31 三期都没推 → 知识库内容落后本地 3 期。
与「README 第 7 节与 docs/20 不同步（25 vs 5）」属同一类问题：**汇总型文档缺回查**。

### 已推送（2 份，W36 前缀，三步流程全部成功）
- `W36-岗位全景与学习路径索引.md`（28322 B）— media_state=2 / parse_progress=100
- `W36-克隆企业级项目测试报告.md`（17406 B）— media_state=2 / parse_progress=100
- 知识库条目 29 → 31

### 踩坑：create_media 不可并行（新发现，已验证）
1. 并行调用 `create_media` 取两份凭证
2. 第 1 份（索引）COS 上传 HTTP 200 成功
3. 第 2 份（测试报告）COS 上传 **HTTP 403 `InvalidAccessKeyId`**
4. 误判为 STS 刚签发、COS 侧同步延迟 → `sleep 8` 重试 → **仍 403**（排除延迟假设）
5. 真实根因：**并发取凭证时第二个 secret_id 与 token 不匹配**
6. 修复：改为串行重新 `create_media` → 立即上传 → 立即 `add_knowledge`，一次成功 HTTP 200

结论：ima 多文件推送必须串行，403 不要靠等待重试。

### 流程固化（已写入自动化任务 automation-1787809410721）
- **B.1b**：累积型文档每期必推，推送前用 `file_size` 比对判断是否过期
- **B.3**：多文件必须串行，写明并行 403 踩坑与「sleep 无效」
- **B.5**：403 报错直接串行重取，不要 sleep 等待
- **B.7**：ima MCP 无删除接口，旧周前缀残留须在 log 列出并提示用户手动清理

### 交付
- `logs/automation-2026-08-31.log` 追加「11:00 ~ 11:15 补推」章节（含根因、踩坑、遗留）
- `.workbuddy/automations/automation-1787809410721/memory.md` 追加 08-31 补推 + 第 4 期 + W35 周报三条
- GitHub：commit 8a4c683 push 成功（44ce613..8a4c683，rc=0）

### 遗留（需人工处理）
ima MCP 无删除接口，知识库中以下两份 08-28 旧版无法自动清理，与 W36- 新版并存会污染 RAG 检索
（同时命中新旧两版 → 答案矛盾）：
- `W35-岗位全景与学习路径索引.md`（13758 B）
- `W35-克隆企业级项目测试报告.md`（11000 B）

建议：在 ima 端手动删除，或将累积型文档改为固定 title（不带周前缀）+ REPLACE 策略，保持单一权威版本。

### 教训
1. 「推送」前先查知识库现状，用 file_size 比对，不要盲目重推
2. 累积型文档（原地追加）与快照型文档（daily-digest）推送策略要分开规定
3. ima 的 STS 凭证不可并发获取，403 不等自愈，直接串行重取
4. 没有删除接口的知识库，推送命名策略要考虑「旧版本残留」的长期成本
